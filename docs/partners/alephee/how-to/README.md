# Alephee — How-to guides

> *Task-oriented.* Recipes for accomplishing a specific goal, assuming you already know the basics.

## Planned guides *(stubs — to be written)*

- [ ] **Onboard a new vendor/marketplace** — which adapters to implement, where they register.
- [ ] **Run the QA regression suite** — `./gradlew cucumber -Dclient=demo`, read the Allure report.
- [ ] **Authenticate to GitHub Packages** — set up `.npmrc` with a `read:packages` PAT for `@alephee/*`.
- [ ] **Cut a release of `vendors-sdk`** — Conventional Commit → semantic-release flow.
- [ ] **Run a Bees adapter migration** — `yarn migrations:up` / `yarn migrations:down`.
- [ ] **Add a translation key in ui-core** — see `ui-core/docs/how-to/add-translation-key.md`.

> Template for each guide: *Goal (one line) → Prerequisites → Steps → Verify → Troubleshooting.*
> Link to the authoritative `ui-core/docs/how-to/` guides instead of copying them.
